---
import { getCollection } from 'astro:content';

const allPosts = await getCollection('blog');

const voyages = {};
allPosts.forEach(post => {
  const voyage = post.data.voyage;
  if (voyage) {
    if (!voyages[voyage]) {
      voyages[voyage] = {
        slug: voyage,
        articles: [],
        latestDate: post.data.pubDate
      };
    }
    voyages[voyage].articles.push(post);
    if (post.data.pubDate > voyages[voyage].latestDate) {
      voyages[voyage].latestDate = post.data.pubDate;
    }
  }
});

const voyageNames = {
  'japon-2025': 'Japon 2025',
  'italie-2024': 'Italie 2024',
  'test': 'Articles de Test',
};

const voyageEmojis = {
  'japon-2025': '🇯🇵',
  'italie-2024': '🇮🇹',
  'test': '✈️',
};
---

<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Mon Blog Voyage</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Merriweather:wght@300;400;700&family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: 'Open Sans', sans-serif;
      background: linear-gradient(135deg, #fdfbfb 0%, #f7f3f0 100%);
      min-height: 100vh;
      color: #2c2416;
    }
    
    .hero {
      text-align: center;
      padding: 80px 20px 60px;
      background: linear-gradient(135deg, #d4a574 0%, #c89666 100%);
      color: #fff;
      position: relative;
      overflow: hidden;
    }
    
    .hero::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.05'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
      opacity: 0.3;
    }
    
    .hero-content {
      position: relative;
      z-index: 1;
      max-width: 800px;
      margin: 0 auto;
    }
    
    .hero h1 {
      font-family: 'Merriweather', serif;
      font-size: 3.5em;
      font-weight: 700;
      margin-bottom: 20px;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
      letter-spacing: -1px;
    }
    
    .hero p {
      font-size: 1.3em;
      font-weight: 300;
      opacity: 0.95;
      letter-spacing: 0.5px;
    }
    
    .container {
      max-width: 1200px;
      margin: -40px auto 0;
      padding: 0 20px 80px;
      position: relative;
      z-index: 2;
    }
    
    .section-title {
      font-family: 'Merriweather', serif;
      font-size: 2em;
      color: #5d4e37;
      margin-bottom: 40px;
      text-align: center;
    }
    
    .voyages-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
      gap: 30px;
    }
    
    .voyage-card {
      background: #fff;
      border-radius: 16px;
      padding: 0;
      text-decoration: none;
      color: inherit;
      transition: all 0.3s ease;
      box-shadow: 0 4px 20px rgba(0,0,0,0.08);
      overflow: hidden;
      position: relative;
    }
    
    .voyage-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 6px;
      background: linear-gradient(90deg, #d4a574, #c89666);
      transform: scaleX(0);
      transform-origin: left;
      transition: transform 0.3s ease;
    }
    
    .voyage-card:hover::before {
      transform: scaleX(1);
    }
    
    .voyage-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 12px 40px rgba(0,0,0,0.15);
    }
    
    .voyage-card-content {
      padding: 40px 35px;
    }
    
    .voyage-emoji {
      font-size: 3.5em;
      display: block;
      margin-bottom: 20px;
      filter: drop-shadow(2px 2px 4px rgba(0,0,0,0.1));
    }
    
    .voyage-card h2 {
      font-family: 'Merriweather', serif;
      font-size: 1.8em;
      color: #5d4e37;
      margin-bottom: 15px;
      font-weight: 700;
    }
    
    .voyage-meta {
      display: flex;
      align-items: center;
      gap: 20px;
      color: #8b7355;
      font-size: 0.95em;
      margin-top: 20px;
      padding-top: 20px;
      border-top: 1px solid #f0ebe6;
    }
    
    .voyage-count {
      display: flex;
      align-items: center;
      gap: 6px;
    }
    
    .voyage-date {
      display: flex;
      align-items: center;
      gap: 6px;
    }
    
    footer {
      text-align: center;
      padding: 40px 20px;
      color: #8b7355;
      font-size: 0.9em;
    }
    
    @media (max-width: 768px) {
      .hero h1 {
        font-size: 2.5em;
      }
      
      .hero p {
        font-size: 1.1em;
      }
      
      .voyages-grid {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>
  <div class="hero">
    <div class="hero-content">
      <h1>✨ Journal de voyage ✨</h1>
      <p>Chroniques de nos pérégrinations.</p>
    </div>
  </div>
  
  <div class="container">
    <div class="voyages-grid">
      {Object.entries(voyages)
        .sort(([,a], [,b]) => b.latestDate - a.latestDate)
        .map(([slug, data]) => (
        <a href={`/voyage/${slug}`} class="voyage-card">
          <div class="voyage-card-content">
            <span class="voyage-emoji">{voyageEmojis[slug] || '✈️'}</span>
            <h2>{voyageNames[slug] || slug}</h2>
            <div class="voyage-meta">
              <span class="voyage-count">
                📝 {data.articles.length} article{data.articles.length > 1 ? 's' : ''}
              </span>
              <span class="voyage-date">
                🗓️ {data.latestDate.toLocaleDateString('fr-FR', { month: 'short', year: 'numeric' })}
              </span>
            </div>
          </div>
        </a>
      ))}
    </div>
  </div>
  
  <footer>
    On accepte les suggestions pour la destination suivante !
  </footer>
</body>
</html>
